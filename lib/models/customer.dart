class Customer {
  String id, name, email, password;
  DateTime createdAt;

  Customer({
    required this.id,
    required this.name,
    required this.email,
    required this.password,
    required this.createdAt,
  });

  addCustomer(String text, String text2, String text3) {}
}
